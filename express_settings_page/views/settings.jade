include ./mixins/url_item
include ./mixins/category_item
include ./mixins/allowed_time_item

extends navbar_fixed_top

block content
	.container.main
		each category, index in categories
			.jumbotron.categories
				label.control-label.editH2(for="category")
					h2.text-info= category.name + " "
				label.control-label.editH2(for="remainingTime")
					small.text-info= category.time
				each url in url_lists[index]
					.input-group
						span.input-group-btn.editUrl
							button.btn.btn-default(type="button") Edit!
						input.form-control(type="text", value= url.name, disabled='')
						span.input-group-btn.deleteUrl
							button.btn.btn-default(type='button') Delete!
					// /input-group
				button.btn.btn-default.addUrl(type='button') +








